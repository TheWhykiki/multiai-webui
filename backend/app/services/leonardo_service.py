class LeonardoService:
    def __init__(self, api_key):
        self.api_key = api_key

    def generate_image(self, prompt):
        # Hier würde die tatsächliche Implementierung für den Aufruf der Leonardo API stehen
        return f"Leonardo image generated with prompt: {prompt}"